const state = {
  authenticated: false,
};

const mutations = {};

const actions = {};

const getters = {};

const category = {
  namespaced: true,
  state: () => state,
  mutations,
  actions,
  getters,
};

export default category;
